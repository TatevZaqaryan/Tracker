Habit Tracker React Project
===========================
Run locally:
1. npm install
2. npm start

This is a frontend-only app that stores data in localStorage per month.
Chart.js and react-chartjs-2 are used for the chart.
